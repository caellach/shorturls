{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Go Server",
            "type": "shell",
            "command": "go build -o __debug_go_shorturlserver.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}